#include "main.h"
#include <stdio.h>
/**
*_sqrt_recursion - returns square root of a number.
*@n: the number.
*@k: the iteration number
*Return: squreroot.
*/
int _sqrt_recursion(int n)
{
return (_sqrt(n, 1));
}
